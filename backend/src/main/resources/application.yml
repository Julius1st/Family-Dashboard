spring:
  application:
    name: backend

  datasource:
    url: jdbc:h2:file:./data/family-dashboard
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
